<%- include( "partials/header.ejs") -%>
<h1>Home</h1>
<p><%= content%></p>
<% articles.forEach(article => { %>
    <h1><%= article.title %></h1>
    <p class="">
        <%= article.body.substring(0,100) + "..." %> 
        <a class="articles" href=" <%- "/show/" + article.id %> "><%= article.id %>   Read More</a>
    </p>
<% }) %>

<%- include("partials/footer.ejs") -%>